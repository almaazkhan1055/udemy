/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cms-images.udemycdn.com",
      "img-c.udemycdn.com",
      "images.udemycdn.com",
    ],
  },
};

export default nextConfig;
